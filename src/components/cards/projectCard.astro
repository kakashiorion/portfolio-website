---
interface Props {
    title:string;
    subtitle:string;
    logoUrl:string;
    githubUrl:string;
    visitUrl:string;
    tools:string[];
}

const { title, subtitle, logoUrl, githubUrl, visitUrl, tools} = Astro.props
---

<a class="card" href={visitUrl}>
    <div class="headline">
        <p class="title">{title}</p>
        <img src={logoUrl} class="icon-large"/>
    </div>
    <p class="subtitle">{subtitle}</p>
    <div class="footer">
        <div class="tools">
            {tools.map((t)=>{
                return <img src={t} class="icon-small"/>
            })}
        </div>
        <img src="/right.svg" class="icon-small"/>
    </div>
</a>

<style>
    .card{
        display: flex;
        padding: 16px;
        flex-direction: column;
        align-items: flex-start;
        gap: 16px;
        align-self: stretch;
        border-radius: 16px;
        background: white;
        box-shadow: 0px 2px 2px 0px rgba(255, 232, 201, 0.50);
    }
    .headline {
        display: flex;
        justify-content: space-between;
        align-items: center;
        align-self: stretch;
        gap:12px;
    }
    .icon-large{
        height: 28px;
        width: 28px;
    }
    .icon-small{
        height:16px;
        max-width:16px;
    }
    .title {
        color: #000;
        font-family: 'Inter';
        font-size: 16px;
        font-style: normal;
        font-weight: 700;
        line-height: normal;
    }
    .subtitle{
        color: #9E7947;
        font-family: 'Inter';
        font-size: 14px;
        font-style: normal;
        font-weight: 400;
        line-height: normal;
        width: 90%;
        padding-bottom: 8px;
    }
    .footer{
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        align-self: stretch;
    }
    .tools{
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 8px;
    }

    @media (min-width: 800px) {
        .card{
            padding: 24px;
            gap: 24px;
            justify-content: space-between;
            align-items: flex-start;
            flex: 1 0 0;
        }
        .icon-large{
            height: 40px;
            width: 40px;
        }
        .icon-small{
            height:20px;
            max-width:20px;
        }
        .title{
            font-size: 22px;
        }
        .subtitle{
            font-size: 18px;
            padding-bottom: 12px;
        }
        .tools{
            gap:12px;
        }
    }

</style>